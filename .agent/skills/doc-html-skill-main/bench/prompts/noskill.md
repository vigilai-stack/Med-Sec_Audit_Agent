Create comprehensive, branded HTML documentation for the system described below.

Requirements:
- Produce a SINGLE self-contained file named `doc.html` in the current directory.
- Include: a styled hero header, an executive summary, at least 3 Mermaid diagrams (architecture flow, request sequence, and risk view), and at least 2 tables (e.g. risks and tech stack).
- Style it with inline CSS using a clean brand theme (a primary accent color, readable typography, cards/sections). Mermaid may be loaded from a CDN.
- Write the final HTML file. Do not ask follow-up questions; use only the facts provided.

When finished, the deliverable is the `doc.html` file.

--- SYSTEM FACTS ---
{{BRIEF}}
