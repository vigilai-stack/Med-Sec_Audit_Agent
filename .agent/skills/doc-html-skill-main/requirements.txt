# Python dependencies for the branded HTML documentation skill suite.
# Install into the project venv:
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt
jinja2>=3.1
jsonschema>=4.18
pyyaml>=6.0
referencing>=0.30
# Optional — PDF export (export_pdf.py) and HTML validation layers:
#   pip install playwright && playwright install chromium
playwright>=1.40
